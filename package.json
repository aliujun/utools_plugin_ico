{
  "name": "png-to-ico",
  "version": "0.0.1",
  "scripts": {
    "dev": "vite",
    "build": "vite build --mode lib && vite build && node scripts/prepare-plugin.js",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^4.2.3",
    "vite": "^4.4.9",
    "vue": "^3.3.0",
    "terser": "^5.24.0"
  }
}